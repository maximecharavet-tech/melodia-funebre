/* ═══ CONFIGURATION MELODIA ═══
   Mode démo (par défaut) : laissez vide — comptes et commandes en localStorage.
   Mode production : créez un projet gratuit sur supabase.com puis remplissez :  */
window.MELODIA_CONFIG = {
  SUPABASE_URL: '',      // ex : 'https://xxxx.supabase.co'
  SUPABASE_ANON_KEY: ''  // Settings → API → anon public
};
